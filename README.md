# alias-tool

## Features

support alias `@import "path";` for scss files

## Extension Settings

`alias-tool.mappings`:

```
{
    "alias-tool.mappings": {
        "app": "frontend/app",
        "@client": "frontend/app",
        "~@client": "frontend/app"
    }
}
```
